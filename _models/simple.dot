digraph simple {

    graph[volume=1.0];

    // Here are the pools.
    a [ conc_init = 1 ];
    b [ conc_init = 0 ];

    // here are the reactions.
    r1 [ kf = 2.0, kb = 1.0 ];

    a -> r1 -> b, b;

}
